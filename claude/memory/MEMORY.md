# Memory index

- [Agents hub structure](agents-hub-structure.md) - ~/.agents is the canonical skills/instructions hub, synced via dotfiles + install-agents.sh; never create real skill dirs in ~/.claude/skills
